# Версия системы, которая была установлена изначально. 
# Это значение генерируется автоматически в файле configuration.nix в самом низу.
# ТРОГАТЬ ЕГО НЕЛЬЗЯ.

{ config, lib, pkgs, ... }: {
    system.stateVersion = "23.11";
}
